#pragma once

namespace beacon {
namespace internal {

// FlushThread logic is implemented within Tracker.cpp as member functions
// of the Tracker class. This header exists for documentation purposes and
// to match the PRD file list. The flush loop runs as Tracker::flush_thread_loop().

} // namespace internal
} // namespace beacon
